/*
 * @class ${name}Layout
 * @author ${user} - ${email}
 * @created ${date}
 *
 */

${name}.${name}Layout = Backbone.Marionette.Layout.extend({
	template: '${name}Layout',

	events: {},

	regions: {
		content : '.content'
	},

	onRender: function() {}

});