/*
 * @class ${name}Layout
 * @author ${user} - ${email}
 * @created ${date}
 *
 */

${namespace}.${namespace}Layout = Backbone.Marionette.Layout.extend({
	template: '${namespace}Layout',

	events: {},

	regions: {
		content : '.content'
	},

	onRender: function() {}

});